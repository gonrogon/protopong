if (CPACK_GENERATOR MATCHES "DEB")
    set(CPACK_DEBIAN_PACKAGE_SHLIBDEPS  ON)
    set(CPACK_DEBIAN_PACKAGE_MAINTAINER "gonrogon <gonrogon@gmail.com>")
    set(CPACK_DEBIAN_PACKAGE_SECTION    "Games")
    set(CPACK_DEBIAN_PACKAGE_HOMEPAGE   "https://github.com/gonrogon/protopong")
endif()

if (CPACK_GENERATOR MATCHES "RPM")
    set(CPACK_RPM_PACKAGE_AUTOREQ ON)
    set(CPACK_RPM_PACKAGE_URL     "https://github.com/gonrogon/protopong")
    set(CPACK_RPM_PACKAGE_LICENSE "MIT")
endif()

if (CPACK_GENERATOR MATCHES "NSIS")
    set(CPACK_NSIS_HELP_LINK      "https://github.com/gonrogon/protopong")
    set(CPACK_NSIS_URL_INFO_ABOUT "https://github.com/gonrogon/protopong")
endif()
